# --- Large raw data (regenerable from IHME GHDx) ---
archive.zip
data/ihme.csv
data/HAQ.CSV
data/SDI_1950_2021.csv
data/ihme_data/
data/ne_110m/
data/iran_shapefile/
data/who_tb_outcomes.csv

# --- Large derived analytic intermediate (>100 MB, regenerable) -----------------
# qci_complete_data.csv is the merged GBD long-form dataset with the three
# component ratios joined per location-year-sex-age. It is produced from
# data/ihme.csv via code/notebooks/2.cleaning_data.ipynb and
# 3.pca_analysis.ipynb. Excluded from the repository because it exceeds
# GitHub's 100 MB per-file limit; users who want to re-run analyses that
# depend on it should regenerate it locally (see README).
results/shared/qci_complete_data.csv

# --- Derived / intermediate model artefacts ---
code/qci_pca_model.joblib

# --- Python ---
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
.venv/
venv/
env/

# --- OS / editor ---
.DS_Store
.vscode/
.idea/
*.swp

# --- LaTeX build artefacts (keep .tex and final .pdf, drop intermediates) ---
*.aux
*.log
*.out
*.toc
*.bbl
*.blg
*.fls
*.fdb_latexmk
*.synctex.gz

# --- Working scratch ---
*.tmp
*~

# --- Internal scaffolding (NEVER commit; kept locally for project workflow) ---
TODO_FOR_USER.md
FIX_PROGRESS.md
CITATIONS_VERIFIED.md

# --- Internal documentation (thesis drafts, proposal, references) ---
# All of docs/ is private project material. References subfolder contained
# publisher PDFs (copyrighted), removed via filter-repo on 2026-05-18.
docs/

# --- Manuscripts (unsubmitted; kept locally for journal submission workflow) ---
# Removed from public repo on 2026-05-18 because they are pre-submission
# drafts. Including them in the Zenodo deposit would constitute a preprint,
# which some target journals (e.g. Archives of Iranian Medicine) may flag
# as prior publication. Local copies backed up under
# ~/Desktop/thesis-internal-backup-20260518-053008/papers/
papers/

# --- Derived/intermediate data files (NEVER commit; regenerable via run_all.sh) ---
# Removed from public repo on 2026-05-18 per conservative interpretation of
# IHME GBD data licensing — derived CSVs/JSONs may contain GBD-derived values
# that are not appropriate for redistribution. Local copies backed up under
# ~/Desktop/thesis-internal-backup-20260518-053008/results-data/
results/**/*.csv
results/**/*.json
results/shared/qci_uncertainty.txt
